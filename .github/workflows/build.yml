---
name: "Build"
on:
  pull_request:
  push:
jobs:
  pre-job:
    continue-on-error: true
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      should_skip: ${{ steps.skip_check.outputs.should_skip }}
    steps:
      - id: skip_check
        uses: fkirc/skip-duplicate-actions@04a1aebece824b56e6ad6a401d015479cd1c50b3
        with:
          concurrent_skipping: "same_content_newer"
          cancel_others: "true"
  find-jobs:
    runs-on: ubuntu-latest
    needs: pre-job
    permissions:
      contents: read
    if: needs.pre-job.outputs.should_skip != 'true'
    outputs:
      packages: ${{ steps.x86_64.outputs.packages }}
      tests: ${{ steps.tests.outputs.tests }}
    steps:
      - uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-nix
      - name: Find package jobs
        id: x86_64
        run: echo "packages=$(nix eval .#hydraJobs.packages.x86_64-linux --apply "builtins.attrNames" --json)" >> "$GITHUB_OUTPUT"
      - name: Find test Jobs
        id: tests
        run: echo "tests=$(nix eval .#hydraJobs.tests --apply "builtins.attrNames" --json)" >> "$GITHUB_OUTPUT"
  build:
    runs-on: ${{ matrix.arch.runner }}
    needs: find-jobs
    permissions: {}
    strategy:
      matrix:
        package: ${{ fromJSON(needs.find-jobs.outputs.packages) }}
        arch:
          - id: x86_64-linux
            runner: blacksmith-8vcpu-ubuntu-2404
          - id: aarch64-linux
            runner: blacksmith-8vcpu-ubuntu-2404-arm
    steps:
      - uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-nix
      - name: Build
        run: |
          nix build \
            --print-build-logs \
            -v \
            .#hydraJobs.packages.${ARCH}.${MATRIX_PACKAGE}
        env:
          MATRIX_PACKAGE: ${{ matrix.package }}
          ARCH: ${{ matrix.arch.id }}
